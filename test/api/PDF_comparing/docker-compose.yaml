services:
  back_end:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: PDF_Comparison
    ports:
      - "8006:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app 
    restart: unless-stopped